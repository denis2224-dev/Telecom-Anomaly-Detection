# Sergiu Day 05–06: voice episode and cause evidence

Verified on 24 September 2026 on `sergiu/days-5-6-g1`. The live G1 code revision was `e3ac942b2261c6b51cf0d7b2e2a1aca3ec73e13f`, with PR #11 runtime base `4d59f6cb38af0f7946b101316ab0c17854c30494` and Ion's final PR #12 evidence commit `a0425ac76348f60d3af011f5843157ec9c66c552` in its ancestry.

## Result

- The processor persists a per-scope episode state and transactional KPI/detection delivery outbox. The second adjacent eligible breach opens one episode. Replay does not advance its sequence or create another delivery. A missing service minute becomes UNKNOWN; three healthy minutes produce RECOVERY.
- The G1 opening is HIGH severity. Aligned, complete SERVICE and `IMS-A` observations with SIP 503 count at least 50, IMS CPU at least 90%, and RRC/bearer setup at most 0.5 percentage points below baseline add an IMS capacity correlation. These cutoffs are required fields in versioned `service-rules-v2`; the voice episode and cause evidence use the same policy snapshot. The probable cause is MEDIUM confidence. Missing or incomplete source evidence leaves confidence LOW.
- The real G1 fixture has no transport measurement, so its opening reports `mlStatus=INSUFFICIENT_DATA`. Three normal control minutes create KPI history without an episode.

## Verification

- `./mvnw.cmd -q clean test` with JDK 21: 189 Java tests passed, including policy boundaries, modified-policy behavior, incomplete source evidence, Testcontainers PostgreSQL migration, durable delivery, replay, and normal control.
- `scripts/check-contracts.py`: passed; Python root suite: 19 passed; ML service suite: 9 passed.
- Dashboard: 25 unit tests passed and production build succeeded.
- `git diff --check`: clean.

## Live G1 browser replay

- UTC window: `2026-09-24T15:08:00Z` to exclusive `2026-09-24T15:16:00Z`; no prior VoLTE receipts in the interval. `runId: N/A — local G1 is CLI-driven`.
- Command: `npm.cmd --prefix apps/dashboard run test:g1 -- --config playwright.g1.local.config.ts`, with `G1_WINDOW_START=2026-09-24T15:08:00Z`. The local Playwright config routed `telecom.test` to the isolated Docker stack through a loopback proxy because Windows had no DNS entry for that hostname. It did not mock API responses.
- Result: PASS. The scenario published through Kafka; processing persisted 16 observation receipts, 8 KPI windows, and 6 detection/evidence updates. Episode `61bcbf099c97e480f526305fe297d8be3e0b60835e750228e0b7c1cba5a45c74` created incident `0068a180-4963-405a-8ac9-244399eb4143`. Phases: OPEN, UPDATE, UNKNOWN, UPDATE, UPDATE, RECOVERY.
- The OPEN detection was HIGH, `mlStatus=INSUFFICIENT_DATA`, `causeConfidence=MEDIUM`, and reported probable IMS capacity pressure. `IMS_CAPACITY_CORRELATION` named `IMS-A`, SIP 503 count 80, IMS CPU 94%, and the aligned SERVICE/IMS-A source event IDs `c9d28dff-251b-336b-9e68-0aeba0d5eb9e` and `d98c6120-03c1-30d0-890f-9fd389a1464f`.
- Real Keycloak login, protected API and dashboard incident display, and detection/KPI history parity passed. Anonymous access and post-logout access returned JSON 401. Exact scenario replay drained through Kafka with the same 16 receipts, KPI history, detection/evidence history, episode, and incident; no second incident appeared.

The run used an isolated Compose project and a temporary local DNS routing shim. Owner acceptance remains pending.

## Follow-up verification on the current voice branch

Verified on 24 September 2026 after reapplying the reverted PR #14 change to `feature/voice-kpi-incident-investigation` at `39cafcd8f0a6b579fbd150eb7c42701b3e0bb943`, then applying the policy and test fixes. The fresh live code revision was `1ab5855` on `sergiu/days-5-6-g1-followup`. Ion's final streaming evidence commit `a0425ac76348f60d3af011f5843157ec9c66c552` is already in the base ancestry; its evidence file and the newer incident-service work were retained unchanged.

- Full processor reactor: 189 Java tests passed. Independent incident service `mvnw.cmd -q -f services/incident-service/pom.xml verify`: 87 tests passed across unit and integration reports. Contract check passed; root Python: 19 tests; ML Python: 9 tests. Dashboard: 25 unit tests and production build passed. `git diff --check` passed.
- Fresh UTC window: `2026-09-24T15:49:00Z` to exclusive `2026-09-24T15:57:00Z`, with zero existing VoLTE receipts before generation. `runId: N/A — local G1 is CLI-driven`.
- Command: `npm.cmd --prefix apps/dashboard run test:g1 -- --config playwright.g1.local.config.ts`, with `G1_WINDOW_START=2026-09-24T15:49:00Z`. PASS: one real authenticated Playwright test in 38.5 seconds, using an isolated Compose stack and temporary local DNS routing proxy, with no API mocks.
- Incident `a284801f-58dc-4f96-a8ba-1d4be1fcebcd`, episode `10cd5270580682318e2891808673445e802c3c527c32b8fd498ba1a8afd24d71`: 16 observation receipts, 8 KPI windows, 6 detection/evidence updates, and phases OPEN, UPDATE, UNKNOWN, UPDATE, UPDATE, RECOVERY. The incident's technical state was RECOVERED; analyst workflow remained OPEN.
- OPEN was HIGH, `mlStatus=INSUFFICIENT_DATA`, `causeConfidence=MEDIUM`, with probable IMS capacity pressure and `IMS_CAPACITY_CORRELATION` for `IMS-A`, SIP 503 count 80 and IMS CPU 94%. Its aligned SERVICE and IMS-A source event IDs were `b1b05fd1-0319-3036-aa43-2700bbb6b7e5` and `cc40a8d0-82f7-3ed1-a204-a8d32d96cf3b`.
- Anonymous and post-logout protected API requests returned JSON 401. Real Keycloak login, protected API and dashboard incident display, KPI/detection parity, and exact replay passed. Replay left the 16 receipts, 8 KPI windows, 6 detection/evidence updates, episode, and incident unchanged; no second incident appeared.

The follow-up branch has not been merged. Owner review remains pending.

## PR #16 acceptance contract follow-up

Verified on 24 September 2026. The acceptance scenario now records the same three IMS cause cutoffs as `service-rules-v2`, and the Python contract test compares them to the canonical policy. The existing recovery mutation test passed unchanged: making minute 5 non-healthy resets the streak, so minute 7 cannot reach its expected RECOVERY phase.

- `scripts/check-contracts.py`: passed. Root Python: 19 passed; ML Python: 9 passed. Processor reactor: 189 Java tests passed; incident service: 87 passed. Dashboard: 25 unit tests passed; production build passed. `git diff --check`: clean.
- Fresh live G1 on code commit `08e4b4222f981e7f269f879800751005e2b80398`: UTC `2026-09-24T16:30:00Z` to exclusive `2026-09-24T16:38:00Z`. Zero prior VoLTE receipts. `runId: N/A — local G1 is CLI-driven`.
- Command: `npm.cmd --prefix apps/dashboard run test:g1 -- --config playwright.g1.local.config.ts`, with `G1_WINDOW_START=2026-09-24T16:30:00Z`. One real Playwright test passed in 43.3 seconds on the isolated Compose stack; a temporary local proxy routed `telecom.test` without API mocks.
- Incident `81ff892f-233b-449b-bc8e-921aa679a6ec`, episode `83c385cc4208d1d0e45921932c39804bc9b66cfa4ee9de8e84ba404bbe67b59a`: 16 observation receipts, 8 KPI windows, 6 detection/evidence updates. Phases: OPEN, UPDATE, UNKNOWN, UPDATE, UPDATE, RECOVERY. Technical state RECOVERED; analyst status OPEN.
- OPEN remained HIGH, `mlStatus=INSUFFICIENT_DATA`, `causeConfidence=MEDIUM`, with probable IMS capacity pressure and `IMS_CAPACITY_CORRELATION`. The aligned SERVICE and IMS-A source event IDs were `2293143f-ca25-38b7-a1bd-1d17a6f90c03` and `5e89a898-b0fe-3fc2-b486-937a3de25393`.
- Anonymous and post-logout protected API requests returned 401. Real Keycloak login, protected API and dashboard incident display, detection/KPI parity, and exact Kafka replay passed. Replay left the same 16 receipts, 8 KPI windows, 6 detection/evidence updates, episode, and incident; no second incident appeared.
