# Sergiu Day 05–06: voice episode and cause evidence

Verified locally on 23 September 2026 on `sergiu/days-5-6-g1`, combining the current G1 runtime branch (`feature/voice-kpi-incident-investigation`) and streaming acceptance branch (`feature/g1-streaming-slice`).

## Result

- The processor persists a per-scope episode state and a transactional KPI/detection delivery outbox. The second adjacent eligible breach opens one episode. Replay does not advance its sequence or create another delivery. A missing service minute becomes UNKNOWN; three healthy minutes produce RECOVERY.
- The G1 opening is HIGH severity. Aligned, complete SERVICE and `IMS-A` observations with SIP 503 count at least 50, IMS CPU at least 90%, and healthy RRC/bearer setup add an IMS capacity correlation. The probable cause is explicitly MEDIUM confidence. Missing or misaligned source evidence leaves confidence LOW.
- The real G1 fixture has no transport measurement, so its opening reports `mlStatus=INSUFFICIENT_DATA`. Three normal control minutes create KPI history without an episode.

## Fresh checks

- `./mvnw.cmd -q clean test` with JDK 21: 185 Java tests passed, including Testcontainers PostgreSQL migration, durable delivery, source evidence, replay, and normal control.
- Targeted `VoiceEpisodeTest,VoiceDeliveryTest` rerun after the final assertion: passed.
- `scripts/check-contracts.py`: passed; Python root suite: 19 passed; ML service suite: 9 passed.
- Dashboard: 25 unit tests passed and production build succeeded.
- `git diff --check`: clean.

The combined branch's live browser replay was not rerun. The runtime branch's earlier real G1 browser result is recorded in [its evidence](2026-09-22-g1-ui.md); this record reports only the fresh checks above. Owner acceptance remains pending.
