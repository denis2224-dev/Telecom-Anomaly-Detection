{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/telecom/events/v1/sms-v1.schema.json",
  "title": "SMS payload v1.0",
  "description": "One synthetic logical message reported once at its final delivery outcome.",
  "type": "object",
  "additionalProperties": false,
  "required": ["messageId", "direction", "destinationCountry", "deliveryStatus", "networkNodeId"],
  "properties": {
    "messageId": {"type": "string", "pattern": "^SMS-[A-Z0-9]+(-[A-Z0-9]+)*$", "maxLength": 64, "description": "Synthetic logical message identity, independent of SMS segment count."},
    "direction": {"type": "string", "enum": ["INBOUND", "OUTBOUND"], "description": "Direction relative to the envelope subscriber."},
    "destinationCountry": {"type": "string", "pattern": "^[A-Z]{2}$", "minLength": 2, "maxLength": 2, "description": "ISO 3166-1 alpha-2 country of the receiving endpoint."},
    "deliveryStatus": {"type": "string", "enum": ["DELIVERED", "FAILED"], "description": "Final delivery result; pending and intermediate retry states are not separate records."},
    "networkNodeId": {"type": "string", "pattern": "^NODE-[A-Z0-9]+(-[A-Z0-9]+)*$", "maxLength": 64, "description": "Synthetic serving node at the final delivery result."}
  }
}
