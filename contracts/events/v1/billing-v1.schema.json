{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/telecom/events/v1/billing-v1.schema.json",
  "title": "BILLING payload v1.0",
  "description": "One posted synthetic charge.",
  "type": "object",
  "additionalProperties": false,
  "required": ["transactionRef", "amountMinor", "currency"],
  "properties": {
    "transactionRef": {"type": "string", "pattern": "^TXN-[A-Z0-9]+(-[A-Z0-9]+)*$", "maxLength": 64, "description": "Business transaction ID for this subscriber, separate from eventId.", "not": {"pattern": "\\s"}},
    "amountMinor": {"type": "integer", "minimum": 0, "maximum": 9007199254740991, "description": "Charge in minor units; 12345 means 123.45 MDL. Refunds are outside v1.0."},
    "currency": {"type": "string", "const": "MDL", "description": "ISO 4217 currency. v1.0 supports MDL only."}
  }
}
